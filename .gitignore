# Build
build/
.gradle/
out/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# NOTE: gradle/wrapper/gradle-wrapper.jar is intentionally committed (NOT ignored).
# It is required for ./gradlew (Linux/macOS) and gradlew.bat (Windows) to run after
# a fresh clone. Excluding it broke first-time builds on every OS (GitHub issue #2).

# Test files
test_*.py
__pycache__/

# Local config
*.local

# Secrets / local env for the mcp-use client (keep .env.example, ignore real .env)
.env
clients/mcp-use/.venv/
